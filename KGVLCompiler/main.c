#include <stdio.h>
#include <Windows.h>
#include <direct.h>
#define MIN_ARG_COUNT 2

int main(int argCount, char* args[])
{
    if (argCount < MIN_ARG_COUNT)
    {
        return 0;
    }

    _mkdir()

    return 0;
}